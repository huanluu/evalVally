name: omr-gate-cleanup-model-comparison
evals:
  - ../evals/omr-gate-cleanup/eval.yaml

matrix:
  model:
    path: /defaults/model
    values:
      - gpt-5.6-sol
      - claude-haiku-4.5

baseline:
  model: gpt-5.6-sol

execution:
  workers: 1
